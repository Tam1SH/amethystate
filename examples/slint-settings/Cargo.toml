[package]
name = "rpstate-slint-settings-example"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
rpstate = { path = "../../crates/rpstate" }
serde = { version = "1.0", features = ["derive"] }
slint = "1.16.1"

[build-dependencies]
slint-build = "1.16.1"
